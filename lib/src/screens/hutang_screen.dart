import 'package:flutter/material.dart';

class HutangScreen extends StatelessWidget {
  const HutangScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Column(
        children: const [
          Text("Hutang Screen"),
        ],
      ),
    );
  }
}